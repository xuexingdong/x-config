spring:
  datasource:
    driver-class-name: org.postgresql.ds.PGSimpleDataSource
    url: jdbc:postgresql://localhost:5432/x?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: postgres
    password: 123456
  rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest
      virtual-host: x
  lettuce:
    host: localhost
    port: 6379
    jedis:
      pool:
        max-active: 200
        max-idle: 50
        min-idle: 10
        max-wait: 300ms
    timeout: 0ms

token:
  secret-key: xxd
  expire-time: 0