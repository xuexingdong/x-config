spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/x
    username: postgres
    password: 123456
  rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest
      virtual-host: x
  redis:
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 200
        max-wait: 300ms
        max-idle: 50
        min-idle: 10
    timeout: 0ms

jwt:
  secret-key: xuexingdong
  expiration: 1m
